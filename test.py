import pygame, sys
pygame.init()
"""
def pressing(buttonposition, button, mouseposition):
    if (buttonposition[0]) < mouseposition[0] < (buttonposition[0] + (button.get_rect()).width) and (buttonposition[1]) < mouseposition[1] < (buttonposition[1] + (button.get_rect()).height): return True
    else: return False

if True:
    screen     = pygame.display.set_mode((1536,800))
    text =  ((pygame.font.SysFont('arial', 32)).render('GeeksForGeeks', True, (0, 35, 0), (0, 200, 0)))
    screen.blit(text, (50, 50))
    print(pressing((50,50), text, (51,51)))
    while True:
        pygame.display.update()
else:
    print('cool')
    b = pygame.Rect(100,100,100,100)
    c = b.collidepoint((51,51))
    print(c)


"""

try:
    print("t1")
    m = t
except: print("t2")




